export const getDevices = (state) => state.input.devices;

export const getSelectedDevice = (state) => state.input.selectedDevice;

export const getSocketPrevious = (state) => state.input.socket.previous;

export const getSocketSettings = (state) => state.input.socket.settings;

export const getSocketStatus = (state) => state.input.socket.status;
